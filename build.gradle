plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'com.gradle'
version = '0.0.1'


java {

}

repositories {
}

dependencies {
}
publishing {
    publications {
        library(MavenPublication) {
            from components.java
        }
    }
    repositories {
        maven {
            url = uri("http://localhost:8081/dinhnguyendev/spring-core-gradle")
			credentials {
				username = "username"
				password = "password"
			}
            allowInsecureProtocol = true
        }
    }
}